seed: 42

data:
  image_size: 224
  batch_size: 32
  num_workers: 4

model:
  teacher: resnet50
  student: vit_small
  pretrained: true

training:
  epochs: 5
  lr: 0.0003
  weight_decay: 0.0001
  device: cuda

output:
  checkpoint_dir: checkpoints/warmup